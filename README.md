# minddrill-rag
MindDrill — RAG chatbot with hybrid retrieval (pgvector + BM25 + RRF), local cross-encoder reranking, async ingestion via Celery, and a sync streaming query path. Built with FastAPI, LangGraph, and self-hosted Langfuse observability + Ragas eval gates.
